# Web app env. In production the API is same-origin, so these can be left unset
# (the app calls /api/... on the current host). For local development against a
# separate API server, set them:
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_WS_URL=ws://localhost:3000
